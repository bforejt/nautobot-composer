# =============================================================================
# Nautobot Apps (Plugins) — Nautobot 3.x Compatible Versions
# =============================================================================
# Each line uses >= (lower bound) and < (upper bound) to constrain pip to the
# correct major version for Nautobot 3.x.  Without these bounds, pip backtracks
# through many candidates, making builds slow or failing with
# "resolution-too-deep".
#
# The Dockerfile also pins nautobot core via a constraints file to prevent
# silent upgrades.
#
# To add an App:
#   1. Uncomment or add the pip package below.
#   2. Add the app's Python module name to PLUGINS in nautobot_config.py.
#   3. Rebuild:  docker compose build --no-cache && docker compose up -d
#
# Recommendation: Enable ONE app at a time and verify Nautobot starts cleanly
# before adding the next.
# =============================================================================

# --- Nornir Plugin for Nautobot (required by Golden Config) ---
nautobot-plugin-nornir>=2.3.0,<4.0.0

# --- Nornir Nautobot (library, not a Nautobot App — no PLUGINS entry needed) ---
#nornir-nautobot>=3.0.0,<4.0.0

# --- Golden Config ---
nautobot-golden-config>=3.0.3,<4.0.0

# --- Single Source of Truth (SSoT) ---
nautobot-ssot>=4.2.1,<5.0.0

# --- Device Lifecycle Management ---
nautobot-device-lifecycle-mgmt>=4.1.0,<5.0.0

# --- BGP Models ---
nautobot-bgp-models>=3.1.0,<4.0.0

# --- Firewall Models ---
nautobot-firewall-models>=3.0.0,<4.0.0

# --- Circuit Maintenance ---
nautobot-circuit-maintenance>=3.1.0,<4.0.0

# --- Design Builder ---
nautobot-design-builder>=3.0.0,<4.0.0

# --- Device Onboarding ---
nautobot-device-onboarding>=5.0.0,<6.0.0

# --- Data Validation Engine ---
# Included in Nautobot 3.0 core — no separate app needed.
# For Nautobot 2.x, see requirements-2.x.txt.

# --- ChatOps (version 4.x for Nautobot 3.0 — different from the others) ---
#nautobot-chatops>=4.0.0,<5.0.0
